import React from 'react';

import classes from './Signature.css';

const signature = (props) => (
	<div className = {classes.signature}>
		<h1 className = {classes.signatureText}>Hi, I'm Adrian</h1>
	</div>
);
export default signature;